{
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build && esbuild ./output/Main/index.js --bundle --outfile=./output/bundle.js --global-name=Main",
    "start": "spago build && esbuild ./output/Main/index.js --bundle --outfile=./output/bundle.js --global-name=Main --serve --servedir=."
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "esbuild": "^0.13.9",
    "purescript": "^0.14.5",
    "purescript-psa": "0.8.2",
    "spago": "^0.20.3"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
